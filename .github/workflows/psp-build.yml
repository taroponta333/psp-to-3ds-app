name: 3DS Homebrew Build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # 1. あなたのコード（main.c や Makefile）を取得
    - name: Checkout code
      uses: actions/checkout@v4

    # 2. 【最重要】GitHub公式の仕組みを使って、3DS開発環境をエラーなしで完璧に構築
    - name: Set up devkitPRO ARM (3DS)
      uses: devkitpro/setup-devkitpro-action@v1

    # 3. コンパイルを実行
    - name: Compile Homebrew
      run: |
        make
          
    # 4. 出来上がった成果物（.3dsx）を保存してダウンロードできるようにする
    - name: Upload compiled binary
      uses: actions/upload-artifact@v4
      with:
        name: 3ds-homebrew-file
        path: |
          *.3dsx
          *.cia
